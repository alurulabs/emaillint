{
  "name": "emaillint",
  "license": "MIT",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspace emaillint-core && npm run build --workspace @emaillint/cli",
    "test": "npm run build && npm test --workspaces",
    "typecheck": "npm run build --workspace emaillint-core && npm run typecheck --workspaces"
  }
}
